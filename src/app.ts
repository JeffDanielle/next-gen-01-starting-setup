const userName = "Jeff";
// const cannot be reinstantiated
// userName = 'Jeff';
let age = 24;

age = 25;

function add(a: number, b: number) {
    let result;
    result = a + b;
    return result;
}

if (age > 20) {
    var isOld = true;
}

// console.log(isOld);
